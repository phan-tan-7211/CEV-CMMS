# Equipment model ownership

Equipment-specific normalization, canonicalization and suggestion logic lives here. This is feature-domain logic, not a generic shared component concern.

Consumers outside this feature use `src/features/equipment/index.ts`.
